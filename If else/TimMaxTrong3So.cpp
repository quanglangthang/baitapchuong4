#include <iostream>
using namespace std;

int main() {
	int a, b, c, max;
	cout << "Nhap 3 so a,b,c: ";
	cin >> a >> b >> c;
	if (a > b) {
		max = a;
	}
	else {
		max = b;
	}
	if (max < c) {
		
	}
	cout << max;
}
